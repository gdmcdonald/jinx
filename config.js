// ─────────────────────────────────────────────
//  Jinx config — edit this file to brand your
//  own instance, then update links.js with your
//  own shortcuts.
// ─────────────────────────────────────────────

const CONFIG = {
  // Displayed in the browser tab and page header
  title: "Jinx",
  // Subtitle shown next to the title in the header (set to null to hide)
  subtitle: "Just Links",
  // Emoji or icon string shown beside the title (set to null to hide)
  emoji: "🔗",

  // URL to your links.js file on GitHub (used for the "Edit links" button)
  // Set to null to hide the edit button
  editUrl: "https://github.com/gdmcdonald/jinx/edit/main/links.js",

  // Your custom domain (must match CNAME file). Set to null if using GitHub Pages URL only.
  customDomain: "jinx.fyi",

  // Your GitHub Pages repo name (the part after github.io/...)
  // e.g. for "myorg.github.io/jinx" this would be "jinx"
  repoName: "jinx",

  // Pico CSS accent colour — options: red, pink, fuchsia, purple, violet,
  // indigo, blue, azure, cyan, jade, green, lime, yellow, amber, orange, sand, grey, zinc, slate
  accentColor: "violet",

  // Analytics — GoatCounter site name (e.g. "jinx" for jinx.goatcounter.com)
  // Set to null to disable
  goatCounter: jinx,

};
